#pragma once

namespace vkr {

enum class EditorWorkspacePreset { Scene, LookDev, Debug, Compact };
enum class ContentBrowserViewMode { Grid, List };
enum class GizmoOperation { Select, Translate, Rotate, Scale };
enum class GizmoSpace { Local, World };

} // namespace vkr
